# Frontend env vars are exposed to the browser at build time.
# Never put secrets here. Backend secrets belong in backend/.env.

VITE_API_BASE_URL=http://localhost:4000/api/v1
VITE_SITE_NAME="Institute for Policy, International Studies & History"
VITE_SITE_SHORT_NAME="IPISH"
VITE_SITE_URL=http://localhost:5173

# Analytics + error tracking (use real values per environment)
VITE_PLAUSIBLE_DOMAIN=
VITE_SENTRY_DSN=
