# Top-level convenience env. The real configuration lives in
# frontend/.env.example and backend/.env.example. Copy each into a
# matching .env in its folder before running.

# Postgres credentials used by docker-compose.yml
POSTGRES_USER=research
POSTGRES_PASSWORD=research
POSTGRES_DB=research_irq
