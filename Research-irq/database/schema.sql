-- Generated snapshot of the current database schema.
-- Regenerate after every migration:
--   pg_dump --schema-only --no-owner --no-privileges $DATABASE_URL > database/schema.sql
-- Integration tests assert this file matches the live DB shape.

-- (Snapshot will be populated once migrations are applied locally.)
